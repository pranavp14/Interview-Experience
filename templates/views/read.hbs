<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Expeience</title>

    <!-- Main css -->
    {{!-- <link rel="stylesheet" href="/experience.css"> --}}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
   <link rel="stylesheet" href="/css/read.css"> 
</head>
{{>header}}
<body>
<div class="main">
    {{>navbar}}
    {{!-- {{>nav}} --}}
    <div class="container">
          <div id="spaceNewline" data-aos="fade-up" data-aos-delay="200">
               {{#each sData}}
                <div class="post-box">  
              <h3 class="post-title"><strong></strong>{{{CompanyName}}} Interview Experience for {{jobRole}} ({{campus}})</strong></h3>
              <div class="meta">
                <span class="post-date">{{this.placeDate}}</span>
                <span class="post-author">  {{{this.name}}}</span>
              </div>
               </div>
               {{/each}}

               {{#each rData}}
                  <div class="post-box">
                   <strong>Round :  {{round}}</strong>
                   <p id="linebreak">{{{experience}}}</p>
                  </div>
              {{/each}}

               {{#each sData}}
                    <div class="post-box">
                   <strong>Messages:</strong>
                   <p>{{{message}}}</p>
                   <strong>Resources:</strong>
                   <p>{{{source}}}</p>
                   </div>
               {{/each}}
              {{!-- <a href="blog-details.html" class="readmore stretched-link"><span>Read More</span><i class="bi bi-arrow-right"></i></a> --}}
          </div>
     </div>
</div>
</body>
</html>